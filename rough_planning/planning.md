

# ex6 planning:

command-like workflow:
```c
/ctx  //shows context
/new [name] // creates a new empty context named `name`
/fork [name] // forks a context named `name`
/cache name  //caches context explicitly
/goto name  // sets the current context equal to checkpoint "name"
/s // li
/ls
```


## UI structure:
- commands
- command-autocomplete

## Required features:
- see token-threshold at all times
- type commands at any time (even when AI is responding)
- multiple agents running at once
- swap between ctx-windows really fast/easily


```
-------------------


terminal / output


------------------
/help
-------------------
command autocomplete

```

