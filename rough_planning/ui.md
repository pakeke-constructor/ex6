

## UI:

It's important to have terminal-history.
(Rich supports this thankfully)


When we swap between "modes", our program should automatically re-render all of the message history. (then render the rich `live` stuff at the bottom)





