

## Task:
clear description of what we are trying to achieve here.

This may NEVER be edited by agent. Only by human. Prevents drift.



<plan>
Agent can freely write stuff here.
Agent writes anything they want; this spot is HIGHLY ephemeral.

This is kinda like the agent's "high level overview" of the plan.
<plan>


<log>
<!-- Append-only. Entry types: TASK, DONE, FINDING, LEARNING, 
SOLUTION, PROPOSAL, APPROVED, DENIED, QUESTION, ANSWER -->
[ISO8601] Openrouter doesn't pass anthropic request params. use `extraParams` param instead.
[ISO8601] Prompt caching for gemini needs warmup. keep in mind for testing.
[ISO8601] [HUMAN]: It needs to be done this way
[ISO8601] Prompt caching for gemini needs warmup. keep in mind for testing.
</log>


<tasks>
  <task status="pending" confidence="0.8" created-by="agent" created-at="ISO8601">
    Description.
  </task>
</tasks>


