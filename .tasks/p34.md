# TASK: Split task workflow into planner agent + coder agent

<plan>

1. In _ex6/agents/_ex6_agents.py, create a "planner" Context:
   - Read-only tools: read_file, glob, search, read_headers, read_body, explore_agent, web_search, websearch_agent
   - Task tools: task_focus, task_create, task_read, task_write_plan, task_add_log, task_query_logs, task_list
   - NO write tools (no write_file, edit_file, edit_file_lines, bash)
   - Has its own PLANNER_SYSTEM_PROMPT that instructs it to plan, not code
   - Uses ANALYTICAL_MODEL (or a smart model suitable for planning)

2. Remove task_create and task_write_plan from the coder contexts' tool lists.
   Coder keeps: task_focus, task_read, task_add_log, task_query_logs, task_list
   (Coder can read tasks, log progress, but doesn't create/plan them)

3. Write PLANNER_SYSTEM_PROMPT:
   - Role: read-only planning agent
   - Explores codebase, researches, creates task, writes detailed plan
   - Cannot write code; plan should be detailed enough for a coding agent to implement
   - Output: task file with filled-in plan section

</plan>


<log>
[2026-03-18T15:04:56Z] [CREATED] Split task workflow into planner agent + coder agent
</log>


<meta>
status: open
created_at: 2026-03-18T15:04:56Z
</meta>
